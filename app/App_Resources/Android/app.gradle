// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.semocapstoneexample"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
